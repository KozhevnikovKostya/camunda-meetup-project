spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meetup?useLegacyDatetimeCode=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
camunda.bpm.admin-user:
  id: demo
  password: demo

